---
import type { HTMLAttributes } from "astro/types";
import { HUBSPOT_CONFIG, STUDIO_CONTACT } from "@/consts.ts";
import Button from "@/components/Button.astro";
import Input from "@/components/Form/Input.astro";
import Textarea from "@/components/Form/Textarea.astro";
import Heading from "@/components/Typography/Heading.astro";
import Paragraph from "@/components/Typography/Paragraph.astro";

interface Props extends HTMLAttributes<"div"> {
  render?: boolean;
}
const { render = true, class: className, ...rest } = Astro.props;
interface CoveragePackage {
  hours: string;
  price: number;
  label: string;
}

const corporatePackages: readonly CoveragePackage[] = [
  { hours: "2-3", price: 450, label: "2-3 hours" },
  { hours: "4-5", price: 700, label: "4-5 hours" },
  { hours: "6-7", price: 1100, label: "6-7 hours" },
  { hours: "8-9", price: 1400, label: "8-9 hours" },
  { hours: "10", price: 1650, label: "10 hours" },
  { hours: "20", price: 3000, label: "Two days (20 hours total)" },
] as const;

const communityPackages: readonly CoveragePackage[] = [
  { hours: "2-3", price: 300, label: "2-3 hours" },
  { hours: "4-5", price: 450, label: "4-5 hours" },
  { hours: "6-7", price: 700, label: "6-7 hours" },
  { hours: "8-9", price: 1100, label: "8-9 hours" },
  { hours: "10", price: 1500, label: "10 hours" },
  { hours: "20", price: 2800, label: "Two days (20 hours max)" },
] as const;

const privatePackages: readonly CoveragePackage[] = [
  { hours: "2", price: 350, label: "2 hours" },
  { hours: "4-5", price: 500, label: "4-5 hours" },
  { hours: "6-7", price: 750, label: "6-7 hours" },
  { hours: "8-9", price: 1000, label: "8-9 hours" },
  { hours: "10", price: 1200, label: "10 hours" },
] as const;
---

{
  render && (
    <div class:list={["pricing-form", className]} {...rest}>
      <form
        id="pricing-form"
        action={STUDIO_CONTACT.formEndpoint}
        method="post"
        data-hubspot-portal={HUBSPOT_CONFIG.portalId}
        data-hubspot-form={HUBSPOT_CONFIG.formId}
        data-hubspot-region={HUBSPOT_CONFIG.region}
      >
        <div class="pricing-form_layout">
          <aside class="pricing-form_sidebar">
            <div class="pricing-form_header">
              <Heading tag="h1" variant="h2" class="pricing-form_title">
                Event photography pricing
              </Heading>
              <Paragraph variant="small" class="pricing-form_desc">
                Choose the type of photography and the time you need. You will
                see an estimate before sending an enquiry. Weddings and larger
                headshot teams are quoted after a short discussion.
              </Paragraph>
            </div>

            <div class="pricing-form_wizard-bar" id="pricing-wizard-bar">
              <div class="pricing-form_wizard-nav">
                <button
                  type="button"
                  class="pricing-form_back-button"
                  id="pricing-back-button"
                  aria-label="Back to previous step"
                  hidden
                >
                  <span aria-hidden="true">&larr;</span>
                  <span>Back</span>
                </button>
                <span
                  class="pricing-form_wizard-step"
                  id="pricing-wizard-step"
                  aria-live="polite"
                >
                  Step 1 of 3: Event type
                </span>
              </div>
              <div
                class="pricing-form_progress-track"
                role="progressbar"
                aria-valuemin="0"
                aria-valuemax="100"
                aria-valuenow="33"
              >
                <div
                  class="pricing-form_progress-fill"
                  id="pricing-progress-fill"
                  style="width: 33.33%;"
                />
              </div>
            </div>

            <div class="pricing-form_step-context" id="pricing-step-1-context">
              <div class="pricing-form_step-heading">
                <span class="pricing-form_number">1</span>
                <div class="pricing-form_step-copy">
                  <Heading tag="h2" variant="h4" id="pricing-type-heading">
                    What do you need photographed?
                  </Heading>
                  <Paragraph variant="small">
                    Choose the closest option. Add any details below.
                  </Paragraph>
                </div>
              </div>
            </div>

            <div class="pricing-form_step-context" id="pricing-step-2-context" hidden>
              <div class="pricing-form_step-heading">
                <span class="pricing-form_number">2</span>
                <div class="pricing-form_step-copy">
                  <Heading tag="h2" variant="h4" id="pricing-options-heading">
                    Choose your coverage
                  </Heading>
                  <Paragraph variant="small" id="pricing-options-intro">
                    Select a coverage block. Extra time is $175 per hour.
                  </Paragraph>
                </div>
              </div>
            </div>

            <div class="pricing-form_step-context" id="pricing-step-3-context" hidden>
              <div class="pricing-form_step-heading">
                <span class="pricing-form_number" id="pricing-step-3-number">3</span>
                <div class="pricing-form_step-copy">
                  <Heading tag="h2" variant="h4" id="pricing-details-heading">
                    Tell me about the job
                  </Heading>
                  <Paragraph variant="small">
                    Share what you know. Venue and organisation are optional.
                  </Paragraph>
                </div>
              </div>

              <div
                id="pricing-summary"
                class="pricing-form_summary"
                aria-live="polite"
                hidden
              >
                <span class="pricing-form_summary-label">
                  Estimated photography fee
                </span>
                <strong id="pricing-total" />
                <p id="pricing-summary-text" />
              </div>
            </div>
          </aside>

          <div class="pricing-form_main">
            <section
              class="pricing-form_stage"
              id="pricing-type-step"
              aria-labelledby="pricing-type-heading"
            >
              <div class="pricing-form_choices pricing-form_choices-types">
                <label class="pricing-form_choice">
                  <input
                    type="radio"
                    name="photographyType"
                    value="corporate"
                    required
                  />
                  <span>Corporate events</span>
                </label>
                <label class="pricing-form_choice">
                  <input
                    type="radio"
                    name="photographyType"
                    value="community"
                    required
                  />
                  <span>Not-for-profit and community events</span>
                </label>
                <label class="pricing-form_choice">
                  <input
                    type="radio"
                    name="photographyType"
                    value="private"
                    required
                  />
                  <span>Private celebrations</span>
                </label>
                <label class="pricing-form_choice">
                  <input
                    type="radio"
                    name="photographyType"
                    value="headshots"
                    required
                  />
                  <span>Workplace headshots</span>
                </label>
                <label class="pricing-form_choice">
                  <input
                    type="radio"
                    name="photographyType"
                    value="wedding"
                    required
                  />
                  <span>Weddings and elopements</span>
                </label>
              </div>
            </section>

            <section
              class="pricing-form_stage"
              id="pricing-options"
              aria-labelledby="pricing-options-heading"
              hidden
            >
              <div id="corporate-pricing" class="pricing-form_packages" hidden>
                <div class="pricing-form_choices pricing-form_choices-prices">
                  {corporatePackages.map((pkg) => (
                    <label class="pricing-form_choice pricing-form_price-choice">
                      <input
                        type="radio"
                        name="coveragePackage"
                        value={pkg.hours}
                        data-label={pkg.label}
                        data-price={String(pkg.price)}
                      />
                      <span>
                        <strong>{pkg.label}</strong>
                        <small>${pkg.price.toLocaleString("en-AU")}</small>
                      </span>
                    </label>
                  ))}
                </div>
              </div>

              <div id="community-pricing" class="pricing-form_packages" hidden>
                <div class="pricing-form_choices pricing-form_choices-prices">
                  {communityPackages.map((pkg) => (
                    <label class="pricing-form_choice pricing-form_price-choice">
                      <input
                        type="radio"
                        name="coveragePackage"
                        value={pkg.hours}
                        data-label={pkg.label}
                        data-price={String(pkg.price)}
                      />
                      <span>
                        <strong>{pkg.label}</strong>
                        <small>${pkg.price.toLocaleString("en-AU")}</small>
                      </span>
                    </label>
                  ))}
                </div>
              </div>

              <div id="private-pricing" class="pricing-form_packages" hidden>
                <div class="pricing-form_choices pricing-form_choices-prices">
                  {privatePackages.map((pkg) => (
                    <label class="pricing-form_choice pricing-form_price-choice">
                      <input
                        type="radio"
                        name="coveragePackage"
                        value={pkg.hours}
                        data-label={pkg.label}
                        data-price={String(pkg.price)}
                      />
                      <span>
                        <strong>{pkg.label}</strong>
                        <small>${pkg.price.toLocaleString("en-AU")}</small>
                      </span>
                    </label>
                  ))}
                </div>
              </div>

              <div id="extra-hours-panel" hidden>
                <label
                  class="pricing-form_field pricing-form_extra-hours"
                  for="extra-hours"
                >
                  <span class="field-label text-style-main">Extra hours</span>
                  <small class="pricing-form_help">
                    Add time beyond the selected block at $175 per hour.
                  </small>
                  <input
                    id="extra-hours"
                    name="extraHours"
                    type="number"
                    min="0"
                    step="1"
                    value="0"
                    inputmode="numeric"
                    class="field-control text-style-main"
                  />
                </label>
              </div>

              <div id="headshot-pricing" class="pricing-form_panel" hidden>
                <label class="pricing-form_field" for="team-size">
                  <span class="field-label text-style-main">
                    How many people need headshots?
                  </span>
                  <small class="pricing-form_help">
                    Individuals from $120 (including 3 edited headshots). Teams of 3 to 5 are $450.
                    Six to nine staff is $110 each. Teams of 10 or more are quoted after discussion.
                  </small>
                  <input
                    id="team-size"
                    name="teamSize"
                    type="number"
                    min="1"
                    step="1"
                    inputmode="numeric"
                    class="field-control text-style-main"
                  />
                </label>
              </div>

              <div id="wedding-pricing" class="pricing-form_panel" hidden>
                <Paragraph>
                  Wedding coverage is priced after a discussion about the date,
                  locations, hours and run sheet.
                </Paragraph>
              </div>

              <div class="pricing-form_stage-actions" id="pricing-step-2-actions">
                <Button type="button" id="pricing-step-2-continue" variant="secondary">
                  Continue to details &rarr;
                </Button>
              </div>
            </section>

            <section
              class="pricing-form_stage"
              id="pricing-details"
              aria-labelledby="pricing-details-heading"
              hidden
            >
              <input
                type="hidden"
                name="eventType"
                id="pricing-event-type"
                value=""
              />
              <input
                type="hidden"
                name="coverageHours"
                id="pricing-coverage-hours"
                value=""
              />
              <input
                type="hidden"
                name="estimatedTotal"
                id="pricing-estimated-total"
                value=""
              />
              <input
                type="hidden"
                name="calculatedQuoteSummary"
                id="pricing-quote-summary"
                value=""
              />
              <div class="pricing-form_fields">
                <Input label="Name" name="fullName" autocomplete="name" required />
                <Input label="Email" name="email" variant="email" required />
                <Input label="Phone" name="phone" variant="phone" required />
                <Input
                  label="Company or organisation"
                  name="organisation"
                  autocomplete="organization"
                />
                <Input
                  label="Date"
                  name="eventDate"
                  placeholder="Date or TBC"
                  required
                />
                <Input label="Venue or suburb" name="venue" />
                <Textarea
                  class="pricing-form_field-wide"
                  label="Project details"
                  name="message"
                  minLines={5}
                  placeholder="Event times, location, what you need photographed, and anything else that will help."
                />
              </div>
              <div class="pricing-form_submit-row">
                <Button type="submit" id="pricing-submit">
                  Request a Quote
                </Button>
                <Paragraph variant="small">
                  No payment is taken through this form.
                </Paragraph>
              </div>
              <div class="pricing-form_status-block" id="pricing-success-wrap" hidden>
                <div
                  class="pricing-form_status pricing-form_status-success"
                  id="pricing-success"
                  role="status"
                >
                  <Heading tag="h3" variant="h3" class="pricing-form_success-title">
                    Quote requested
                  </Heading>
                  <Paragraph class="pricing-form_success-desc">
                    Thanks. Your enquiry has been sent. I’ll check the details and get back to you shortly.
                  </Paragraph>
                </div>
                <div class="pricing-form_reset-row">
                  <Button type="button" emphasis="secondary" id="pricing-reset-btn">
                    Start a new enquiry
                  </Button>
                </div>
              </div>
              <p class="pricing-form_status" id="pricing-error" role="alert" hidden>
                That did not send. Email{" "}
                <a href={`mailto:${STUDIO_CONTACT.email}`}>
                  {STUDIO_CONTACT.email}
                </a>{" "}
                or call{" "}
                <a href={`tel:${STUDIO_CONTACT.phoneRaw}`}>
                  {STUDIO_CONTACT.phone}
                </a>
                .
              </p>
            </section>
          </div>
        </div>
      </form>
    </div>
  )
}

<script>
  const typeLabels: Record<string, string> = {
    corporate: "Corporate event",
    community: "Not-for-profit or community event",
    private: "Private celebration",
    headshots: "Workplace headshots",
    wedding: "Wedding or elopement",
  };

  function initPricingForm() {
    const form = document.querySelector<HTMLFormElement>("#pricing-form");
    if (!form || form.dataset.ready === "true") return;
    form.dataset.ready = "true";
    const find = <T extends Element>(selector: string) =>
      form.querySelector<T>(selector);

    const step1 = find<HTMLElement>("#pricing-type-step");
    const step2 = find<HTMLElement>("#pricing-options");
    const step3 = find<HTMLElement>("#pricing-details");
    const step1Context = find<HTMLElement>("#pricing-step-1-context");
    const step2Context = find<HTMLElement>("#pricing-step-2-context");
    const step3Context = find<HTMLElement>("#pricing-step-3-context");
    const backButton = find<HTMLButtonElement>("#pricing-back-button");
    const wizardStep = find<HTMLElement>("#pricing-wizard-step");
    const progressFill = find<HTMLElement>("#pricing-progress-fill");
    const step3Number = find<HTMLElement>("#pricing-step-3-number");
    const step2Continue = find<HTMLButtonElement>("#pricing-step-2-continue");

    const corporatePricing = find<HTMLElement>("#corporate-pricing");
    const communityPricing = find<HTMLElement>("#community-pricing");
    const privatePricing = find<HTMLElement>("#private-pricing");
    const extraHoursPanel = find<HTMLElement>("#extra-hours-panel");
    const headshotPricing = find<HTMLElement>("#headshot-pricing");
    const weddingPricing = find<HTMLElement>("#wedding-pricing");
    const intro = find<HTMLElement>("#pricing-options-intro");
    const summary = find<HTMLElement>("#pricing-summary");
    const total = find<HTMLElement>("#pricing-total");
    const summaryText = find<HTMLElement>("#pricing-summary-text");
    const extraHours = find<HTMLInputElement>("#extra-hours");
    const teamSize = find<HTMLInputElement>("#team-size");
    const hiddenType = find<HTMLInputElement>("#pricing-event-type");
    const hiddenHours = find<HTMLInputElement>("#pricing-coverage-hours");
    const hiddenTotal = find<HTMLInputElement>("#pricing-estimated-total");
    const hiddenSummary = find<HTMLInputElement>("#pricing-quote-summary");
    const submit = find<HTMLButtonElement>("#pricing-submit");
    const successWrap = find<HTMLElement>("#pricing-success-wrap");
    const success = find<HTMLElement>("#pricing-success");
    const error = find<HTMLElement>("#pricing-error");
    const resetBtn = find<HTMLButtonElement>("#pricing-reset-btn");
    const fieldsContainer = find<HTMLElement>(".pricing-form_fields");
    const submitRow = find<HTMLElement>(".pricing-form_submit-row");

    let currentStep = 1;
    let currentType = "";

    const selectedType = () =>
      find<HTMLInputElement>('input[name="photographyType"]:checked')?.value ||
      "";
    const selectedPackage = () =>
      find<HTMLInputElement>('input[name="coveragePackage"]:checked');

    function clearCoverage() {
      for (const input of document.querySelectorAll<HTMLInputElement>(
        '#pricing-form input[name="coveragePackage"]',
      ))
        input.checked = false;
      if (extraHours) extraHours.value = "0";
      if (teamSize) teamSize.value = "";
    }

    function writeSummary(
      label: string,
      description: string,
      fee: string,
      hours = "",
    ) {
      if (total) total.textContent = fee;
      if (summaryText) summaryText.textContent = description;
      if (summary) summary.hidden = false;
      if (hiddenType) hiddenType.value = label;
      if (hiddenHours) hiddenHours.value = hours;
      if (hiddenTotal) hiddenTotal.value = fee;
      if (hiddenSummary)
        hiddenSummary.value = `${label}\n${description}\nEstimated photography fee: ${fee}`;
    }

    function hideSummary() {
      if (summary) summary.hidden = true;
      if (hiddenHours) hiddenHours.value = "";
      if (hiddenTotal) hiddenTotal.value = "";
      if (hiddenSummary) hiddenSummary.value = "";
    }

    function calculate() {
      const type = selectedType();
      const label = typeLabels[type] || "Photography enquiry";
      if (["corporate", "community", "private"].includes(type)) {
        const coverage = selectedPackage();
        if (!coverage) return hideSummary();
        const packageLabel = coverage.dataset.label || coverage.value;
        const basePrice = Number(coverage.dataset.price || 0);
        const addedHours = Math.max(0, Number(extraHours?.value || 0));
        const price = basePrice + addedHours * 175;
        const extraLabel = addedHours
          ? ` plus ${addedHours} extra ${addedHours === 1 ? "hour" : "hours"}`
          : "";
        const note =
          type === "corporate" && coverage.value === "20"
            ? "Review the run sheet before confirming two-day work. Final scope is confirmed before booking."
            : "Final scope is confirmed before booking.";
        writeSummary(
          label,
          `${packageLabel}${extraLabel}. ${note}`,
          `$${price.toLocaleString("en-AU")}`,
          addedHours > 0 ? `${packageLabel} + ${addedHours}h extra` : packageLabel,
        );
        return;
      }
      if (type === "headshots") {
        const count = Math.max(0, Number(teamSize?.value || 0));
        if (!count) return hideSummary();
        if (count >= 10)
          return writeSummary(
            label,
            `${count} people. Quoted after discussion. Two finished headshots per person included.`,
            "Priced after discussion",
            `${count} people`,
          );
        let price = 450;
        let description = "";
        if (count === 1) {
          price = 120;
          description = "1 person. Three edited headshots included.";
        } else if (count === 2) {
          price = 240;
          description = "2 people. Three edited headshots each included.";
        } else if (count <= 5) {
          price = 450;
          description = `${count} people. Small team base with edited headshots included.`;
        } else {
          price = count * 110;
          description = `${count} people at $110 per person. Two finished headshots per person included.`;
        }
        writeSummary(
          label,
          `${description} Final scope is confirmed before booking.`,
          `$${price.toLocaleString("en-AU")}`,
          `${count} people`,
        );
        return;
      }
      if (type === "wedding")
        writeSummary(
          label,
          "Price confirmed after discussing the date, locations, hours and run sheet.",
          "Priced after discussion",
        );
    }

    function updateProgress() {
      const type = selectedType();
      const isWedding = type === "wedding";
      const totalSteps = isWedding ? 2 : 3;

      if (currentStep === 1) {
        if (backButton) backButton.hidden = true;
        if (wizardStep)
          wizardStep.textContent = `Step 1 of ${totalSteps}: Event type`;
        if (progressFill)
          progressFill.style.width = isWedding ? "50%" : "33.33%";
      } else if (currentStep === 2) {
        if (backButton) backButton.hidden = false;
        const stepName = type === "headshots" ? "Team size" : "Coverage";
        if (wizardStep)
          wizardStep.textContent = `Step 2 of ${totalSteps}: ${stepName}`;
        if (progressFill) progressFill.style.width = "66.66%";
      } else if (currentStep === 3) {
        if (backButton) backButton.hidden = false;
        const stepNum = isWedding ? 2 : 3;
        if (wizardStep)
          wizardStep.textContent = `Step ${stepNum} of ${totalSteps}: Your details`;
        if (progressFill) progressFill.style.width = "100%";
        if (step3Number) step3Number.textContent = String(stepNum);
      }
    }

    function goToStep(step: number) {
      currentStep = step;

      if (step === 1) {
        if (step1) step1.hidden = false;
        if (step2) step2.hidden = true;
        if (step3) step3.hidden = true;
        if (step1Context) step1Context.hidden = false;
        if (step2Context) step2Context.hidden = true;
        if (step3Context) step3Context.hidden = true;
      } else if (step === 2) {
        if (step1) step1.hidden = true;
        if (step2) step2.hidden = false;
        if (step3) step3.hidden = true;
        if (step1Context) step1Context.hidden = true;
        if (step2Context) step2Context.hidden = false;
        if (step3Context) step3Context.hidden = true;
      } else if (step === 3) {
        if (step1) step1.hidden = true;
        if (step2) step2.hidden = true;
        if (step3) step3.hidden = false;
        if (step1Context) step1Context.hidden = true;
        if (step2Context) step2Context.hidden = true;
        if (step3Context) step3Context.hidden = false;
        calculate();
      }

      updateProgress();
    }

    if (backButton) {
      backButton.addEventListener("click", () => {
        if (currentStep === 3) {
          if (selectedType() === "wedding") {
            goToStep(1);
          } else {
            goToStep(2);
          }
        } else if (currentStep === 2) {
          goToStep(1);
        }
      });
    }

    if (resetBtn) {
      resetBtn.addEventListener("click", () => {
        form.reset();
        clearCoverage();
        hideSummary();
        currentType = "";
        if (fieldsContainer) fieldsContainer.hidden = false;
        if (submitRow) submitRow.hidden = false;
        if (successWrap) successWrap.hidden = true;
        if (error) error.hidden = true;
        if (submit) {
          submit.disabled = false;
          submit.textContent = "Request a Quote";
        }
        goToStep(1);
      });
    }

    if (step2Continue) {
      step2Continue.addEventListener("click", () => {
        const type = selectedType();
        if (type === "headshots") {
          const count = Number(teamSize?.value || 0);
          if (!count) {
            teamSize?.focus();
            return;
          }
        } else if (["corporate", "community", "private"].includes(type)) {
          if (!selectedPackage()) {
            return;
          }
        }
        calculate();
        goToStep(3);
      });
    }

    form.addEventListener("change", (event) => {
      const target = event.target as HTMLInputElement;
      if (target.name === "photographyType") {
        const type = target.value;
        if (type !== currentType) {
          currentType = type;
          clearCoverage();
        }
        if (corporatePricing) corporatePricing.hidden = type !== "corporate";
        if (communityPricing) communityPricing.hidden = type !== "community";
        if (privatePricing) privatePricing.hidden = type !== "private";
        if (extraHoursPanel)
          extraHoursPanel.hidden = !["corporate", "community", "private"].includes(
            type,
          );
        if (headshotPricing) headshotPricing.hidden = type !== "headshots";
        if (weddingPricing) weddingPricing.hidden = type !== "wedding";
        if (intro)
          intro.textContent =
            type === "headshots"
              ? "Enter the team size."
              : type === "wedding"
                ? "Wedding pricing is confirmed after a short discussion."
                : "Select a coverage block. Extra time is $175 per hour.";
        calculate();

        // Card auto-advance to next layer
        setTimeout(() => {
          if (type === "wedding") {
            goToStep(3);
          } else {
            goToStep(2);
          }
        }, 150);
      } else if (target.name === "coveragePackage") {
        calculate();
        // Card auto-advance to form layer
        setTimeout(() => {
          goToStep(3);
        }, 160);
      } else {
        calculate();
      }
    });

    form.addEventListener("input", (event) => {
      const target = event.target as HTMLInputElement;
      if (target.id === "extra-hours" || target.id === "team-size") calculate();
    });

    form.addEventListener("keydown", (event) => {
      if (event.key === "Enter" && currentStep === 2) {
        const target = event.target as HTMLElement;
        if (target.id === "team-size" || target.id === "extra-hours") {
          event.preventDefault();
          const type = selectedType();
          if (type === "headshots" && Number(teamSize?.value || 0) > 0) {
            calculate();
            goToStep(3);
          } else if (
            ["corporate", "community", "private"].includes(type) &&
            selectedPackage()
          ) {
            calculate();
            goToStep(3);
          }
        }
      }
    });

    for (const input of form.querySelectorAll<HTMLInputElement>(
      'input[type="text"], input[type="email"], input[type="tel"]',
    )) {
      input.addEventListener("blur", () => {
        input.value = input.value.trim();
      });
    }

    form.addEventListener("submit", async (event) => {
      if (!form.checkValidity()) {
        form.reportValidity();
        return;
      }
      event.preventDefault();
      if (submit) {
        submit.disabled = true;
        submit.textContent = "Sending...";
      }
      if (success) success.hidden = true;
      if (error) error.hidden = true;
      const formData = new FormData(form);
      const fullName = String(formData.get("fullName") || "").trim();
      const parts = fullName.split(/\s+/);
      const email = String(formData.get("email") || "").trim().toLowerCase();
      const phone = String(formData.get("phone") || "").trim();
      const company = String(formData.get("organisation") || "").trim();
      const eventDate = String(formData.get("eventDate") || "").trim();
      const venue = String(formData.get("venue") || "").trim();
      const userMessage = String(formData.get("message") || "").trim();

      const photographyType = String(formData.get("eventType") || "");
      const selectedPkg = selectedPackage();
      const coverageLabel =
        selectedPkg?.dataset.label ||
        String(formData.get("coveragePackage") || "");
      const extraHoursSelected = String(formData.get("extraHours") || "0");
      const teamSizeSelected = String(formData.get("teamSize") || "");
      const estimatedTotal = String(formData.get("estimatedTotal") || "");
      const selectionNote = [
        "Website pricing tool selection",
        `Photography type: ${photographyType}`,
        coverageLabel ? `Coverage block: ${coverageLabel}` : "",
        Number(extraHoursSelected) > 0 ? `Extra hours: ${extraHoursSelected}` : "",
        teamSizeSelected ? `Team size: ${teamSizeSelected} people` : "",
        `Estimate shown: ${estimatedTotal}`,
      ]
        .filter(Boolean)
        .join("\n");
      const message = [
        selectionNote,
        `Event date: ${eventDate || "Not supplied"}`,
        `Venue or suburb: ${venue || "Not supplied"}`,
        userMessage ? `Client notes:\n${userMessage}` : "",
      ]
        .filter(Boolean)
        .join("\n\n");
      try {
        let sent = false;
        const portal = form.dataset.hubspotPortal;
        const formId = form.dataset.hubspotForm;
        const region = form.dataset.hubspotRegion || "ap1";
        if (portal && formId) {
          const companyVal = company || "Individual / Private";
          const hsPayload = {
            fields: [
              { name: "firstname", value: parts[0] || "Client" },
              {
                name: "lastname",
                value: parts.slice(1).join(" ") || parts[0] || "Client",
              },
              { name: "email", value: email },
              { name: "phone", value: phone },
              { name: "company", value: companyVal },
              { name: "0-2/name", value: companyVal },
              { name: "message", value: message },
            ],
            context: {
              pageUri: window.location.href,
              pageName: document.title,
            },
          };

          try {
            const response = await fetch(
              `https://api-${region}.hsforms.com/submissions/v3/integration/submit/${portal}/${formId}`,
              {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(hsPayload),
              },
            );
            if (response.ok) {
              sent = true;
            } else {
              const fallbackResponse = await fetch(
                `https://api.hsforms.com/submissions/v3/integration/submit/${portal}/${formId}`,
                {
                  method: "POST",
                  headers: { "Content-Type": "application/json" },
                  body: JSON.stringify(hsPayload),
                },
              );
              sent = fallbackResponse.ok;
            }
          } catch (hsErr) {
            console.warn("HubSpot submission error:", hsErr);
          }
        }
        if (!sent && form.action) {
          const fallbackData = new FormData(form);
          if (!fallbackData.get("fullName")) fallbackData.set("fullName", fullName);
          if (!fallbackData.get("organisation")) {
            fallbackData.set("organisation", company || "Individual / Private");
          }
          if (!fallbackData.get("venue")) {
            fallbackData.set("venue", venue || "TBC / Sydney");
          }
          if (!fallbackData.get("turnaround")) {
            fallbackData.set("turnaround", "Standard (3-5 days)");
          }
          if (!fallbackData.get("coverageHours")) {
            fallbackData.set("coverageHours", coverageLabel || "Not specified");
          }
          if (!fallbackData.get("eventType")) {
            fallbackData.set("eventType", photographyType || "Event photography");
          }
          const response = await fetch(form.action, {
            method: "POST",
            body: fallbackData,
            headers: { Accept: "application/json" },
          });
          sent = response.ok;
        }
        if (!sent) throw new Error("Submission failed");

        if (fieldsContainer) fieldsContainer.hidden = true;
        if (submitRow) submitRow.hidden = true;
        if (successWrap) successWrap.hidden = false;
        if (success) success.hidden = false;
        if (error) error.hidden = true;
        if (backButton) backButton.hidden = true;
        if (wizardStep) wizardStep.textContent = "Quote requested";
        if (progressFill) progressFill.style.width = "100%";
      } catch (err) {
        console.error("Submission failed:", err);
        if (error) error.hidden = false;
        if (submit) {
          submit.disabled = false;
          submit.textContent = "Request a Quote";
        }
      }
    });

    goToStep(1);
  }
  if (document.readyState === "loading")
    document.addEventListener("DOMContentLoaded", initPricingForm);
  else initPricingForm();
  document.addEventListener("astro:page-load", initPricingForm);
</script>

<style is:global>
  @layer components {
    .pricing-form {
      max-width: 72rem;
      margin-inline: auto;
    }
    .pricing-form [hidden] {
      display: none !important;
    }
    .pricing-form_layout {
      display: grid;
      grid-template-columns: minmax(19rem, 23.5rem) minmax(0, 1fr);
      gap: var(--space-8);
      align-items: start;
    }
    .pricing-form_sidebar {
      position: sticky;
      top: var(--space-6);
      display: flex;
      flex-direction: column;
      gap: var(--space-5);
    }
    .pricing-form_header {
      display: flex;
      flex-direction: column;
      gap: 0;
    }
    .pricing-form_title {
      margin: 0;
      margin-bottom: var(--space-4) !important;
      line-height: 1.15;
      overflow-wrap: normal;
      word-break: normal;
      hyphens: none;
    }
    .pricing-form_desc {
      margin: 0;
      color: var(--text-2);
      font-size: var(--text-small);
      line-height: 1.5;
    }
    .pricing-form_wizard-bar {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      padding-block: var(--space-3);
      border-block: var(--border-width) solid var(--border);
    }
    .pricing-form_wizard-nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      min-height: 2.25rem;
    }
    .pricing-form_back-button {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: 0.35rem 0.85rem;
      font-family: inherit;
      font-size: var(--text-small);
      font-weight: 500;
      color: var(--text);
      background: var(--background-2);
      border: var(--border-width) solid var(--border);
      border-radius: var(--radius-small);
      cursor: pointer;
      transition:
        background var(--hover-duration) var(--hover-ease),
        border-color var(--hover-duration) var(--hover-ease);
    }
    .pricing-form_back-button:hover {
      background: var(--background-3);
      border-color: var(--text-2);
    }
    .pricing-form_wizard-step {
      font-size: var(--text-small);
      color: var(--text-2);
      font-weight: 500;
      letter-spacing: 0.02em;
      margin-left: auto;
    }
    .pricing-form_progress-track {
      width: 100%;
      height: 0.375rem;
      background: var(--background-2);
      border-radius: var(--radius-round);
      overflow: hidden;
    }
    .pricing-form_progress-fill {
      height: 100%;
      background: var(--brand-500);
      border-radius: var(--radius-round);
      transition: width 0.25s ease-out;
    }
    .pricing-form_step-context {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      animation: stageFadeIn 0.18s ease;
    }
    .pricing-form_step-heading {
      display: grid;
      grid-template-columns: 2.25rem minmax(0, 1fr);
      gap: var(--space-3);
      align-items: start;
      margin-bottom: 0;
    }
    .pricing-form_step-copy {
      display: flex;
      flex-direction: column;
      gap: 0;
    }
    .pricing-form_step-copy .heading {
      margin: 0;
      margin-bottom: var(--space-2) !important;
      line-height: 1.25;
    }
    .pricing-form_step-copy .text {
      margin: 0;
      color: var(--text-2);
      font-size: var(--text-small);
      line-height: 1.45;
    }
    .pricing-form_number {
      display: grid;
      place-items: center;
      width: 2.25rem;
      height: 2.25rem;
      border: var(--border-width) solid var(--border);
      border-radius: 50%;
      font-weight: 700;
      font-size: var(--text-small);
      background: var(--background-2);
    }
    .pricing-form_main {
      min-width: 0;
      min-height: 24rem;
    }
    .pricing-form_stage {
      animation: stageFadeIn 0.18s ease;
    }
    @keyframes stageFadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    .pricing-form_choices {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: var(--space-3);
    }
    .pricing-form_choices-types .pricing-form_choice:last-child {
      grid-column: 1 / -1;
    }
    .pricing-form_choice {
      position: relative;
      display: block;
      cursor: pointer;
    }
    .pricing-form_choice input {
      position: absolute;
      opacity: 0;
      pointer-events: none;
    }
    .pricing-form_choice > span {
      display: flex;
      flex-direction: column;
      justify-content: center;
      min-height: 4.25rem;
      padding: var(--space-3) var(--space-4);
      border: var(--border-width) solid var(--border);
      border-radius: var(--radius-main);
      background: var(--background-2);
      font-size: 0.9375rem;
      font-weight: 500;
      line-height: 1.35;
      transition:
        background var(--hover-duration) var(--hover-ease),
        color var(--hover-duration) var(--hover-ease),
        border-color var(--hover-duration) var(--hover-ease);
    }
    @media (hover: hover) {
      .pricing-form_choice:hover > span {
        border-color: var(--text-2);
        background: var(--background-3);
      }
    }
    .pricing-form_choice input:checked + span {
      color: var(--background);
      background: var(--text);
      border-color: var(--text);
    }
    .pricing-form_choice input:focus-visible + span {
      outline: 0.125rem solid var(--brand-500);
      outline-offset: 0.1875rem;
    }
    .pricing-form_price-choice > span {
      min-height: 4.5rem;
    }
    .pricing-form_price-choice strong {
      font-size: 1rem;
      line-height: 1.25;
    }
    .pricing-form_price-choice small {
      margin-top: 0.2rem;
      font-size: var(--text-small);
      opacity: 0.85;
    }
    .pricing-form_field {
      display: flex;
      flex-direction: column;
      gap: var(--space-2);
    }
    .pricing-form_help {
      max-width: 100%;
      color: var(--text-2);
      font-weight: 400;
      line-height: 1.45;
      font-size: var(--text-small);
    }
    .pricing-form_extra-hours,
    .pricing-form_panel {
      max-width: 100%;
      margin-top: var(--space-4);
    }
    .pricing-form_extra-hours input {
      max-width: 8rem;
    }
    .pricing-form_stage-actions {
      margin-top: var(--space-4);
      display: flex;
      justify-content: flex-end;
    }
    .pricing-form_summary {
      margin-top: var(--space-2);
      padding: var(--space-4);
      border: var(--border-width) solid var(--border);
      border-radius: var(--radius-main);
      background: var(--background-2);
    }
    .pricing-form_summary-label {
      display: block;
      margin-bottom: var(--space-1);
      font-size: var(--text-small);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--text-2);
    }
    .pricing-form_summary strong {
      display: block;
      font-size: clamp(1.75rem, 5vw, 2.5rem);
      line-height: 1.1;
      color: var(--text);
    }
    .pricing-form_summary p {
      margin: var(--space-2) 0 0;
      font-size: var(--text-small);
      color: var(--text-2);
      line-height: 1.4;
    }
    .pricing-form_fields {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: var(--space-3) var(--space-4);
    }
    .pricing-form_field-wide {
      grid-column: 1 / -1;
    }
    .pricing-form_submit-row {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: var(--space-3);
      margin-top: var(--space-4);
    }
    .pricing-form_submit-row .button_wrap {
      --button-background: var(--brand-500);
      --button-text: var(--brand-text);
      --button-border: var(--brand-500);
      --button-background-hover: var(--text);
      --button-text-hover: var(--background);
      --button-border-hover: var(--text);
    }
    .pricing-form_submit-row .text {
      margin: 0;
      color: var(--text-2);
      font-size: var(--text-small);
    }
    .pricing-form_status-block {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
      margin-top: var(--space-3);
    }
    .pricing-form_status {
      margin-top: var(--space-4);
      padding: var(--space-4);
      border: var(--border-width) solid var(--border);
      border-radius: var(--radius-main);
    }
    .pricing-form_status-success {
      margin-top: 0;
      padding: var(--space-5);
      background: var(--surface-2, rgba(0, 0, 0, 0.02));
      border: var(--border-width) solid var(--border);
      border-radius: var(--radius-main);
    }
    .pricing-form_status-success .heading:has(+ .text),
    .pricing-form_success-title {
      margin-top: 0;
      margin-bottom: var(--space-2) !important;
      color: var(--text);
      line-height: 1.25;
    }
    .pricing-form_success-desc {
      margin: 0;
      margin-top: 0 !important;
      color: var(--text) !important;
      line-height: 1.5;
    }
    .pricing-form_reset-row {
      margin-top: var(--space-1);
    }
    @media (max-width: 52rem) {
      .pricing-form_layout {
        grid-template-columns: 1fr;
        gap: var(--space-5);
      }
      .pricing-form_sidebar {
        position: static;
        gap: var(--space-4);
      }
      .pricing-form_main {
        min-height: auto;
      }
      .pricing-form_choices,
      .pricing-form_fields {
        grid-template-columns: 1fr;
      }
      .pricing-form_choices-types .pricing-form_choice:last-child,
      .pricing-form_field-wide {
        grid-column: auto;
      }
      .pricing-form_step-heading {
        grid-template-columns: 2.25rem minmax(0, 1fr);
        gap: var(--space-2);
      }
      .pricing-form_number {
        width: 2.25rem;
        height: 2.25rem;
      }
    }
  }
</style>
