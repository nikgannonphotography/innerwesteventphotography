# Parked contact page

This folder preserves the contact page that was active before the pricing tool replaced it on 2 September 2026.

The active `/contact` route now contains the pricing tool. The old `/quote-builder` route redirects to `/contact`.
